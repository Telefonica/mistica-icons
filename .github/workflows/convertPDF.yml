name: convert svg to pdf

on:
  push:

jobs:
  assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Convert to PDF
        run: |
          sudo apt-get install inkscape
          inkscape --export-type=pdf -A icons/*.pdf *.svg
